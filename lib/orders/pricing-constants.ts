/** Kansas City area sales tax rate used at checkout (8.8%). */
export const ORDER_TAX_RATE = 0.088;

/** Flat delivery fee in cents when fulfillment is delivery. */
export const ORDER_DELIVERY_FEE_CENTS = 399;

/** Sanity cap for custom tips (server-side). */
export const ORDER_MAX_TIP_CENTS = 50_000;

export const ORDER_MAX_LINE_QUANTITY = 50;
